# Perfect Web

Frontend Angular de Perfect.

## Comandos
```bash
npm ci
npm start
npm run build
```

## API base URL
Se define en `src/environments/environment.ts`:
- `apiBaseUrl` default: `http://localhost:8080/api/v1`

Opcional runtime:
- `window.__env.PERFECT_API_BASE_URL`